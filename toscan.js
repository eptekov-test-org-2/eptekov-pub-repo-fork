// This is a single-line comment in JavaScript.

/*
This is a
multi-line comment
in JavaScript.
*/

// Declare a variable and assign it a value
let message = "Hello from JavaScript!";

// Log the message to the browser's console
console.log(message);

// Define a function
function greetUser(name) {
  return "Welcome, " + name + "!";
}

// Call the function and store the result
let greeting = greetUser("User");

// Display an alert with the greeting
alert(greeting);

// Perform a simple calculation
let num1 = 10;
let num2 = 5;
let sum = num1 + num2;

// Log the sum to the console
console.log("The sum is: " + sum);
